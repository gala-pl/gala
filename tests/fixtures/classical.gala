fn fib(n: Int) -> Int pure {
    if n <= 1 {
        return n
    }
    return fib(n - 1) + fib(n - 2)
}

fn main() -> Int pure {
    let msg = "Hello, Gala!"
    print(msg)
    let result = fib(10)
    print(result)
    return 0
}